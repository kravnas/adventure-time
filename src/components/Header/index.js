import React from "react";
import './index.css';

class Header extends React.Component {
    render() {
      return (
        <header id="header">
            Header;
        </header>
      );
    }
  }

export default Header;

